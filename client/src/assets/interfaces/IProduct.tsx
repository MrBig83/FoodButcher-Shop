export default interface IProduct {
    id: number, 
    title: string,
    description: string,
    usage: string, 
    suits: string, 
    ingredients: string, 
    nutritions: string, 
    price: number,
    image: string,
    instock: number,
    quantity: string
  }